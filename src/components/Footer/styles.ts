import { css } from 'lit';

export default css`
  :host {
    display: block;
  }

  footer {
    width: 100%;
    height: 8rem;
  }

  .footer-left a {
    color: var(--color-primary-dark);
    margin-bottom: 1rem;
    display: block;
  }
`;
